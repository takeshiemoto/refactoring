/**
 * 名前が式以上の事を語っていない場合は
 * その変数がリファクタリングの邪魔になる
 */

const members = [];

// const memberLength = members.length;
//
// if (memberLength === 0) {
//   console.log('Empty!!');
// }

if (members.length === 0) {
  console.log('Empty!!');
}
